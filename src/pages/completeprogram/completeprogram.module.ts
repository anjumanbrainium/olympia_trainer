import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CompleteprogramPage } from './completeprogram';

@NgModule({
  declarations: [
    CompleteprogramPage,
  ],
  imports: [
    IonicPageModule.forChild(CompleteprogramPage),
  ],
})
export class CompleteprogramPageModule {}
